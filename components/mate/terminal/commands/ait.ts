import type { Command } from '../types/commands';

export const aitCommand: Command = {
  name: 'ait',
  description: 'Play with AIt',
  handler: () => `Hey there! I'm _AIt_ (acts like "alt" /ɔːlt/, but also pronounced as "eight" /eɪt/). It depends. 🤷‍♂️`,
};
